saved_queries:
  - name: interview_operational_summary
    description: >
      Executive-level summary of interview operations,
      including interview volume, completion rate,
      and operational efficiency.

    query_params:
      metrics:
        - total_interviews
        - interview_completion_rate
        - avg_interview_duration_hours